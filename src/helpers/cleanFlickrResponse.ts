// import { Response } from "node-fetch";
//
// export const cleanFlickrResponse = async (
//   response: Response
// ): Promise<string> => {
//   return await response.text().then(text => text.slice(14, -1));
// };
